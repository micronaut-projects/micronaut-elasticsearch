micronaut:
  application:
    name: micronaut-elasticsearch-graal
elasticsearch:
  indexes:
    movies: micronaut.movies
test-resources:
  containers:
    elasticsearch:
      startup-timeout: 300s
      image-name: docker.elastic.co/elasticsearch/elasticsearch:9.2.1
